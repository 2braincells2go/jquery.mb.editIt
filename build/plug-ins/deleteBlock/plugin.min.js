! function( a, b ) {
	var c = {
		name: "deleteBlock",
		description: "",
		version: "1.0",
		author: "Pupunzi",
		activate: function() {
			console.debug( "Activate:: ", this );
			var c = this;
			a( b ).on( "editIt-mouseup." + c.name, function( a ) {} )
		},
		update: function() {
			var c = this;
			a( b ).on( "editIt-mousedown." + c.name, function( b ) {
				var c = b.editor,
					d = function() {
						var b = _( "<h2>Do you really want to delete this content?</h2>" );
						a.editIt.prompt.draw( c, b, null, function() {
							a( c ).remove(), setTimeout( function() {
								a.editIt.toolBar.clear( c )
							}, 300 )
						}, null, null, !1 )
					};
				c.deleteBlock = a.editIt.util.drawButton( "remove this block", "align-right main-color editIt-delete-block", "editIt-icon-close only-icon", d ), c.buttonBar && !a( ".editIt-delete-block", c.buttonBar ).length && c.buttonBar.append( c.deleteBlock )
			} )
		},
		destroy: function() {}
	};
	a.editIt.plugins.register( c )
}( jQuery, document );
//# sourceMappingURL=plugin.min.js.map
