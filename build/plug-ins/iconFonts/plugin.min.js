! function( a, b ) {
	var c = {
		name: "iconFont",
		description: "",
		version: "1.0",
		author: "Pupunzi",
		command: {
			label: _( "Add emoticon" ),
			icon: "emoticons-smile",
			type: "dropdown",
			action: function( b ) {
				var c = [ "h1", "h2", "h3", "h4", "p" ];
				a.editIt.dropDown.draw.apply( this, [ b, c ] )
			}
		},
		activate: function() {
			console.debug( "Activate:: ", this );
			var c = this;
			a.editIt.commands.extend( "iconfont", this.command ), a( b ).on( "editIt-mouseup." + c.name, function( a ) {} )
		},
		i18n: {
			"it-IT": {
				"Add emoticon": "Aggiungi un'emoticon"
			}
		}
	};
	a.editIt.plugins.register( c )
}( jQuery, document );
//# sourceMappingURL=plugin.min.js.map
